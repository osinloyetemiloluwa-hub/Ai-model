"""Gateway bearer-token auth tests — removed.

Static atlr_* token auth has been removed from Corvin. This test
module is kept as an empty stub to avoid CI breakage on discovery.

OIDC/JWT auth tests live in test_oidc.py. Local deployments use the
loopback auto-login path (no token needed).
"""
