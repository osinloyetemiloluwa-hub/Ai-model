"""Provider registries for ADR-0033 abstractions.

Each sub-module exposes:
    get_active() -> <ProviderProtocol>
    set_active(provider) -> None

Import only what you need; sub-modules are independent.
"""
