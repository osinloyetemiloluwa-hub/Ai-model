"""Token management + first-run tests — removed.

Static atlr_* bearer tokens have been removed from Corvin.
Local access uses the loopback auto-login path (/auth/local-login).

This module is kept as an empty stub to avoid CI breakage on discovery.
"""
