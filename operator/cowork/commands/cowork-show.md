---
description: Show a persona as JSON (tools, MCP, prompt-append, ...)
argument-hint: "<persona>"
---

Gib die komplette Konfiguration einer Persona aus. Argument: Persona-Name
(z. B. `coder`, `browser`, `research`, `inbox`).

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/cowork" show "$1"
```

Gib das Ergebnis 1:1 wieder.
