---
description: Always summarize replies before reading aloud, regardless of length
argument-hint: ""
---

Switch the voice mode to `summary`. Run:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/voice_cli.sh mode summary
```

Confirm in one sentence that future replies will be summarized first, even short ones.
