---
description: Show the WhatsApp bridge status
argument-hint: ""
---

Show the WhatsApp bridge status: are the daemon and adapter running, is the WhatsApp account paired, whitelist size, pending outbox items.

Run:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/whatsapp_cli.sh status
```

Summarize the result for the user in a single sentence.
