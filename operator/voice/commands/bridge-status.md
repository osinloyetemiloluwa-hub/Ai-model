---
description: Status of all bridges (adapter + every channel) at a glance
argument-hint: ""
---

Shows the live status of every service (adapter, whatsapp, telegram, discord, slack) plus each channel's HTTP health endpoint.

Run:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/bridges/bridge.sh status
```

Summarize which channels are active and which are not.
