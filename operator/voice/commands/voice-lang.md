---
description: Set the TTS language mode — auto, de, or en
argument-hint: "auto|de|en"
---

Set the language mode. Run:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/voice_cli.sh lang "$ARGUMENTS"
```

Confirm to the user which mode is now active.
