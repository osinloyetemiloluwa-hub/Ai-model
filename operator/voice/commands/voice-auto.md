---
description: Use the automatic threshold-based voice mode (short answers read in full, long ones summarized)
argument-hint: ""
---

Switch the voice mode back to `auto`. Run:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/voice_cli.sh mode auto
```

Confirm in one sentence that the threshold-based default is active again.
