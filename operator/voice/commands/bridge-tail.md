---
description: Live log of all bridges combined
argument-hint: ""
---

Tail the journald logs of the adapter and every channel daemon live. End with Ctrl-C.

Run:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/bridges/bridge.sh tail
```

Note to the user: this command blocks the terminal until they end it with Ctrl-C.
